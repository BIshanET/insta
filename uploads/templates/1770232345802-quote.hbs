<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Premium Editorial - HBS Template</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,400;0,700;1,600&family=Oswald:wght@300;500;700&family=Playfair+Display:ital,wght@1,900&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810; 
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
            --font-quote: 'Playfair Display', serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        /* 1080x1350 Portrait Container */
        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            box-shadow: 0 50px 100px rgba(0,0,0,0.9);
        }

        /* LAYER 1: DYNAMIC CENTERED SUBJECT */
        .person-bg {
            position: absolute;
            top: 0; 
            left: 50%;
            transform: translateX(-50%);
            width: 100%;
            height: 80%;
            background-size: cover;
            background-position: center top;
            filter: contrast(1.1) brightness(0.9);
            z-index: 1;
        }

        /* LAYER 2: EDGE BLENDING (Top, Bottom, Left, Right) */
        .edge-blend {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: 2;
            background: 
                linear-gradient(to top, var(--cinema-brown) 25%, transparent 60%),
                linear-gradient(to bottom, var(--cinema-brown) 0%, transparent 20%),
                linear-gradient(to right, var(--cinema-brown) 0%, transparent 15%),
                linear-gradient(to left, var(--cinema-brown) 0%, transparent 15%);
        }

        /* LAYER 3: BLENDED LOGO FOUNDATION */
        .logo-blended {
            position: absolute;
            bottom: -42px;
            left: 2%;
            transform: translateX(-50%);
            width: 1200px;
            height: 900px;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center bottom;
            filter: sepia(1) saturate(5) hue-rotate(-15deg) brightness(0.8) contrast(1.2);
            mix-blend-mode: screen; 
            opacity: 0.25;
            z-index: 3;
        }

        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.15;
            z-index: 4;
            pointer-events: none;
        }

        .gold-frame {
            position: absolute;
            top: 40px; left: 40px; right: 40px; bottom: 40px;
            border: 1px solid rgba(255, 179, 0, 0.2);
            z-index: 5;
            pointer-events: none;
        }

        /* CONTENT AREA */
        .content-area {
            position: relative;
            z-index: 10;
            width: 100%;
            padding: 0 120px 70px 120px;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* QUOTE SYMBOLS */
        .quote-symbol {
            font-family: var(--font-quote);
            font-size: 380px;
            color: var(--cinema-yellow);
            line-height: 0;
            opacity: 0.2;
            position: absolute;
            pointer-events: none;
        }
        .quote-open { top: -60px; left: 20px; }
        .quote-close { bottom: 180px; right: 20px; }

        /* QUOTE TEXT */
        .quote-container {
            margin-bottom: 17px;
            position: relative;
            z-index: 11;
        }

        .quote-text {
            font-family: var(--font-display);
            font-size: 74px;
            color: var(--text-light);
            line-height: 1.05;
            text-transform: uppercase;
            font-weight: 700;
            letter-spacing: -1px;
            text-shadow: 0 15px 30px rgba(0,0,0,0.8);
        }

        .quote-text span.highlight {
            color: var(--cinema-yellow);
            font-style: italic;
        }

        /* ATTRIBUTION */
        .attribution {
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 10px;
            min-width: 550px;
        }

        .person-name {
            font-family: var(--font-display);
            color: var(--cinema-yellow);
            font-size: 68px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 4px;
            line-height: 1;
        }

        .footer-brand {
            position: absolute;
            bottom: 60px;
            width: 100%;
            text-align: center;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.15);
            font-size: 18px;
            letter-spacing: 22px;
            text-transform: uppercase;
            z-index: 10;
        }

    </style>
</head>

<script>
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <!-- Dynamic Background with 4-way edge blend -->
        <div class="person-bg" style="background-image: url('{{image}}');"></div>
        <div class="edge-blend"></div>
        
        <!-- Logo Blended into the Brown Base -->
        <div class="logo-blended"></div>
        
        <div class="texture-overlay"></div>
        <div class="gold-frame"></div>

        <!-- Decorative Quotes -->
        <div class="quote-symbol quote-open">“</div>
        <div class="quote-symbol quote-close">”</div>

        <!-- Main Content Area -->
        <div class="content-area">
            
            <div class="quote-container">
                <h2 class="quote-text">
                    {{{highlight quote}}}
                </h2>
            </div>

            <div class="attribution">
                <div class="person-name">{{name}}</div>
            </div>
        </div>

    </div>

</body>
</html>