<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinema Quotes Stack</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&family=Oswald:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Scaling for Desktop Preview */
        .preview-scale-wrapper {
            transform-origin: center;
        }

        /* --- INSTAGRAM REEL CONTAINER (1080x1920) --- */
        .reel-container {
            width: 1080px;
            height: 1920px;
            background-color: var(--cinema-brown);
            display: flex;
            flex-direction: column; /* Stacks items vertically */
            position: relative;
            overflow: hidden;
        }

        /* Global Texture Overlay */
        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.4;
            pointer-events: none;
            z-index: 50;
        }

        /* --- THE STACK ITEM --- */
        /* flex: 1 makes this div grow to fill available space equally based on number of items */
        .quote-block {
            flex: 1; 
            position: relative;
            overflow: hidden;
            border-bottom: 4px solid var(--cinema-yellow);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .quote-block:last-child {
            border-bottom: none;
        }

        /* Background Image */
        .bg-img {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            object-fit: cover;
            z-index: 1;
        }

        /* Gradient Overlay for Readability */
        .gradient-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(
                to bottom, 
                rgba(44, 24, 16, 0.3) 0%, 
                rgba(44, 24, 16, 0.6) 50%,
                rgba(44, 24, 16, 0.95) 100%
            );
            z-index: 2;
        }

        /* Text Content */
        .content-wrapper {
            position: relative;
            z-index: 10;
            width: 85%;
            text-align: center;
            padding: 20px;
        }

        .quote-text {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: 48px;
            line-height: 1.3;
            text-transform: uppercase;
            font-weight: 700;
            text-shadow: 0 4px 10px rgba(0,0,0,0.8);
        }

        .quote-mark {
            color: var(--cinema-yellow);
            font-size: 1.2em;
            vertical-align: -5px;
            margin: 0 5px;
        }

        .quote-source {
            font-family: var(--font-main);
            color: var(--cinema-yellow);
            font-size: 24px;
            font-weight: 600;
            margin-top: 15px;
            letter-spacing: 2px;
            text-transform: uppercase;
            opacity: 0.9;
        }

        /* --- BRANDING FOOTER --- */
        .branding {
            position: absolute;
            bottom: 40px;
            left: 0;
            width: 100%;
            text-align: center;
            z-index: 100;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.5);
            font-size: 24px;
            letter-spacing: 4px;
            text-transform: uppercase;
        }
    </style>
</head>
<body>

    <div class="preview-scale-wrapper">
        
        <div class="reel-container">
            <div class="texture-overlay"></div>

            <!-- Loop Logic: Iterates through 'quotes' array. 
                 Uses @index to find matching image and movie from the other arrays. -->
            {{#each quotes}}
            <div class="quote-block">
                <!-- Look up image from 'images' array at current index -->
                <img src="{{lookup ../images @index}}" class="bg-img" alt="Movie Scene">
                
                <div class="gradient-overlay"></div>
                
                <div class="content-wrapper">
                    <p class="quote-text">
                        <span class="quote-mark">“</span>
                        {{this}}
                        <span class="quote-mark">”</span>
                    </p>
                    <!-- Look up movie name from 'movies' array at current index -->
                    <p class="quote-source">- {{lookup ../movies @index}}</p>
                </div>
            </div>
            {{/each}}

            <div class="branding">@cinefriendsofficial</div>

        </div>

    </div>

</body>
</html>