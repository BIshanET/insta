<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Hero Template</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Oswald:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        /* 1080x1350 Portrait Container */
        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: #000;
            position: relative;
            display: flex;
            flex-direction: column;
            justify-content: flex-end; 
            overflow: hidden;
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
        }

        /* Main Hero Background Image */
        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .hero-bg img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            filter: contrast(110%) brightness(90%);
        }

        /* The Cinematic Gradient Overlay */
        .overlay-gradient {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(
                to bottom,
                rgba(0, 0, 0, 0) 0%,
                rgba(0, 0, 0, 0.2) 40%,
                rgba(44, 24, 16, 0.8) 80%,
                rgba(20, 10, 5, 1) 100%
            );
            z-index: 2;
        }

        /* Film Texture Overlay */
        .texture-overlay {
            position: absolute; 
            top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.25;
            pointer-events: none;
            z-index: 3;
        }

        /* Content Area */
        .content-section {
            position: relative;
            z-index: 10;
            padding: 0 80px 160px 80px; 
        }

        .post-title {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: 90px;
            text-transform: uppercase;
            letter-spacing: -1px;
            line-height: 1;
            margin-bottom: 20px;
            text-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        span.highlight {
            color: var(--cinema-yellow);
        }

        /* Footer Branding */
        .footer-brand {
            position: absolute;
            bottom: 60px;
            left: 0;
            width: 100%;
            text-align: center;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.4);
            font-size: 24px;
            letter-spacing: 10px;
            text-transform: uppercase;
            z-index: 10;
        }

        /* Subtle Inner Border like a film cell */
        .inner-border {
            position: absolute;
            top: 40px;
            bottom: 40px;
            left: 40px;
            right: 40px;
            border: 1px solid rgba(255, 211, 0, 0.15);
            pointer-events: none;
            z-index: 4;
        }
    </style>
</head>

<script>
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <!-- Hero Image - Updated to reference direct string -->
        <div class="hero-bg">
            {{#if images}}
                <img src="{{images}}" alt="Hero Image">
            {{/if}}
        </div>

        <!-- Visual Layers -->
        <div class="overlay-gradient"></div>
        <div class="texture-overlay"></div>
        <div class="inner-border"></div>

        <!-- Text Content -->
        <div class="content-section">
            {{#if title}}
                <h1 class="post-title">
                    {{{highlight title}}}
                </h1>
            {{/if}}
        </div>

        <!-- Branding -->
        <div class="footer-brand">@cinefriendsofficial</div>
    </div>

</body>
</html>