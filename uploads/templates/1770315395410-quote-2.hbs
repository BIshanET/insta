<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Premium Editorial - HBS</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,400;0,700;1,600&family=Oswald:wght@300;500;700&family=Playfair+Display:ital,wght@1,900&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810; 
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
            --font-quote: 'Playfair Display', serif;
            
            /* Dynamic Font Size Logic */
            --title-size: {{#if titleFontSize}}{{titleFontSize}}px{{else}}92px{{/if}};
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        /* 1080x1350 Portrait Container */
        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            box-shadow: 0 50px 100px rgba(0,0,0,0.9);
        }

        /* LAYER 1: DYNAMIC CENTERED SUBJECT */
        .person-bg {
            position: absolute;
            top: 0; 
            left: 50%;
            transform: translateX(-50%);
            width: 100%;
            height: 85%;
            background-image: url('{{image}}');
            background-size: cover;
            background-position: center top;
            filter: contrast(1.1) brightness(0.8);
            z-index: 1;
        }

        /* LAYER 2: EDGE BLENDING */
        .edge-blend {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: 2;
            background: 
                linear-gradient(to top, var(--cinema-brown) 15%, transparent 50%),
                linear-gradient(to bottom, var(--cinema-brown) 0%, transparent 15%),
                linear-gradient(to right, var(--cinema-brown) 0%, transparent 10%),
                linear-gradient(to left, var(--cinema-brown) 0%, transparent 10%);
        }

        /* LAYER 3: BLENDED LOGO FOUNDATION */
        .logo-blended {
            position: absolute;
            bottom: -42px;
            left: 2%;
            transform: translateX(-50%);
            width: 1200px;
            height: 900px;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center bottom;
            filter: sepia(1) saturate(5) hue-rotate(-15deg) brightness(0.8) contrast(1.2);
            mix-blend-mode: screen; 
            opacity: 0.2;
            z-index: 3;
        }

        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.15;
            z-index: 4;
            pointer-events: none;
        }

        /* CONTENT AREA */
        .content-area {
            position: relative;
            z-index: 10;
            width: 100%;
            padding: 0 100px 50px 100px;
            text-align: center;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* QUOTE CONTAINER */
        .quote-container {
            margin-bottom: 40px;
            position: relative;
        }

        .quote-text {
            font-family: var(--font-display);
            font-size: var(--title-size); 
            color: var(--text-light);
            line-height: 0.95;
            text-transform: uppercase;
            font-weight: 700;
            letter-spacing: -2px;
            text-shadow: 0 10px 20px rgba(0,0,0,0.5);
            position: relative;
            z-index: 2;
        }

        .highlight {
            color: var(--cinema-yellow);
        }

        /* MEDIUM SQUARED QUOTE (Top Left) */
        .quote-symbol {
            font-family: var(--font-display); /* Oswald makes it squared */
            font-size: 160px;
            color: var(--cinema-yellow);
            position: absolute;
            opacity: 0.4;
            z-index: 1;
            line-height: 1;
        }
        
        .quote-open { 
            top: -90px; 
            left: -40px; 
        }

        /* BRANDED ATTRIBUTION */
        .attribution {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 15px;
        }

        .divider {
            width: 60px;
            height: 4px;
            background-color: var(--cinema-yellow);
            margin-bottom: 5px;
        }

        .person-name {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: 42px; 
            font-weight: 500;
            text-transform: uppercase;
            letter-spacing: 8px;
        }

        .footer-brand {
            position: absolute;
            bottom: 50px;
            width: 100%;
            text-align: center;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.2);
            font-size: 16px;
            letter-spacing: 15px;
            text-transform: uppercase;
        }

    </style>
</head>

<script>
  // Handlebars Helper for |highlighting|
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <div class="person-bg" style="background-image: url('{{image}}');"></div>
        <div class="edge-blend"></div>
        <div class="logo-blended"></div>
        <div class="texture-overlay"></div>

        <div class="content-area">
            
            <div class="quote-container">
                <!-- Squared Medium Quote -->
                <span class="quote-symbol quote-open">"</span>
                
                <h2 class="quote-text">
                    {{{highlight quote}}}
                </h2>
            </div>

            <div class="attribution">
                <div class="divider"></div>
                <div class="person-name">{{name}}</div>
            </div>
        </div>

        <div class="footer-brand">Cinefriends Editorial</div>
    </div>

</body>
</html>