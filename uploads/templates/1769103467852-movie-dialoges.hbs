<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinema Subtitle Stack</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&family=Oswald:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* Scaling for Desktop Preview */
        .preview-scale-wrapper {
            transform-origin: center;
        }

        /* --- INSTAGRAM REEL CONTAINER (1080x1920) --- */
        .reel-container {
            width: 1080px;
            height: 1920px;
            background-color: var(--cinema-brown);
            display: flex;
            flex-direction: column; /* Stacks items vertically */
            position: relative;
            overflow: hidden;
        }

        /* Global Texture Overlay */
        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.4;
            pointer-events: none;
            z-index: 50;
        }

        /* --- THE STACK ITEM --- */
        .dialogue-block {
            flex: 1; /* Automatically adjusts height based on number of items */
            position: relative;
            overflow: hidden;
            border-bottom: 4px solid var(--cinema-yellow);
            display: flex;
            justify-content: center;
        }

        .dialogue-block:last-child {
            border-bottom: none;
        }

        /* Background Image */
        .bg-img {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            object-fit: cover;
            z-index: 1;
        }

        /* Gradient Overlay - Focused on the bottom for subtitles */
        .gradient-overlay {
            position: absolute; 
            bottom: 0; left: 0; width: 100%; height: 60%;
            background: linear-gradient(
                to top, 
                rgba(44, 24, 16, 0.95) 0%, 
                rgba(44, 24, 16, 0.5) 40%,
                rgba(44, 24, 16, 0) 100%
            );
            z-index: 2;
        }

        /* Subtitle Container */
        .subtitle-wrapper {
            position: absolute;
            bottom: 45px; /* Positioned at bottom center */
            z-index: 10;
            width: 90%;
            text-align: center;
        }

        /* Subtitle Text Style */
        .subtitle-text {
            font-family: var(--font-main);
            color: var(--cinema-yellow);
            font-size: 42px;
            line-height: 1.4;
            font-weight: 600;
            text-shadow: 2px 2px 0px #000, 0 0 8px rgba(0,0,0,0.8);
            letter-spacing: 0.5px;
        }

        /* White accent span for emphasis if needed */
        .subtitle-text span {
            color: var(--text-light);
        }
    </style>
</head>
<body>

    <div class="preview-scale-wrapper">
        
        <div class="reel-container">
            <div class="texture-overlay"></div>

            <!-- Loop Logic: Iterates through 'texts' array. 
                 Uses @index to lookup the matching image. -->
            {{#each texts}}
            <div class="dialogue-block">
                <!-- Look up image from 'images' array at current index -->
                <img src="{{lookup ../images @index}}" class="bg-img" alt="Scene Image">
                
                <div class="gradient-overlay"></div>
                
                <div class="subtitle-wrapper">
                    <!-- Triple braces allow for HTML like <span> or <br> -->
                    <p class="subtitle-text">{{{this}}}</p>
                </div>
            </div>
            {{/each}}

        </div>

    </div>

</body>
</html>