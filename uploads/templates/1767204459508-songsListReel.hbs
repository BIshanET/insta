<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #1a1a1a;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: 'Helvetica Neue', Arial, sans-serif;
        }

        .reel-canvas {
            width: 1080px;
            height: 1920px;
            background: radial-gradient(circle at center, #F7F0E9 0%, #DED2C5 60%, #A39688 100%);
            display: flex;
            flex-direction: column;
            align-items: center;
            position: relative;
        }

        .safe-zone-content {
            width: 850px;
            margin-top: 350px;
            display: flex;
            flex-direction: column;
        }

        .header {
            display: flex;
            align-items: center;
            margin-bottom: 50px;
            gap: 25px;
        }

        .art-box {
            width: 200px;
            height: 200px;
            border-radius: 30px;
            overflow: hidden;
            box-shadow: 0 15px 30px rgba(0,0,0,0.1);
            background: url('{{coverImage}}');
            background-size: cover;
            background-position: center;
        }

        .header-text h1 {
            color: #B6371A;
            font-size: 70px;
            margin: 0;
            line-height: 0.9;
            font-weight: 800;
        }

        .header-text .label {
            font-size: 24px;
            font-weight: 800;
            margin-top: 20px;
            color: #1a1a1a;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .song-list {
            border-top: 2px solid rgba(0,0,0,0.06);
        }

        .song-row {
            display: flex;
            align-items: center;
            padding: 35px 0;
            border-bottom: 2px solid rgba(0,0,0,0.06);
        }

        .play-circle {
            width: 70px;
            height: 70px;
            background: #B6371A;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            margin-right: 30px;
        }

        .play-circle svg { width: 35px; fill: white; margin-left: 5px; }

        .song-meta { flex-grow: 1; }

        .song-title {
            font-size: 34px;
            font-weight: 700;
            color: #1a1a1a;
            margin-bottom: 5px;
        }

        .song-artist {
            font-size: 26px;
            color: #666;
        }

        .actions {
            display: flex;
            align-items: center;
            gap: 25px;
            padding-right: 20px;
        }

        .heart svg { width: 40px; fill: #B6371A; }
        .more { display: flex; gap: 6px; }
        .dot { width: 8px; height: 8px; background: #333; border-radius: 50%; }

        .footer-logo {
            position: absolute;
            bottom: 450px;
            width: 100%;
            text-align: center;
            font-size: 28px;
            color: rgba(0,0,0,0.4);
            letter-spacing: 4px;
            text-transform: uppercase;
        }
    </style>
</head>
<body>

    <div class="reel-canvas">
        <div class="safe-zone-content">
            <!-- Header -->
            <div class="header">
                <div class="art-box"></div>
                <div class="header-text">
                    <h1>{{headerTitle}}</h1>
                    <div class="label">Top Tracks</div>
                </div>
            </div>

            <!-- Song List -->
            <div class="song-list">
                {{#each songs}}
                <div class="song-row">
                    <div class="play-circle">
                        <svg viewBox="0 0 24 24"><path d="M8 5v14l11-7z"/></svg>
                    </div>
                    <div class="song-meta">
                        <div class="song-title">{{this}}</div>
                        <!-- Lookup artist from artists array at the current loop index -->
                        <div class="song-artist">{{lookup ../artists @index}}</div>
                    </div>
                    <div class="actions">
                        <div class="heart">
                            <svg viewBox="0 0 24 24"><path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09 C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"/></svg>
                        </div>
                        <div class="more">
                            <div class="dot"></div><div class="dot"></div><div class="dot"></div>
                        </div>
                    </div>
                </div>
                {{/each}}
            </div>
        </div>

        <div class="footer-logo">@cinefriendsofficial</div>
    </div>

</body>
</html>