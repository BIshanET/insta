<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Hero - Static Preview</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Oswald:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
            
            /* Logic for titleFontSize variable */
            --title-size: 100px; 
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        /* 1080x1350 Portrait Container */
        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: #000;
            position: relative;
            display: flex;
            flex-direction: column;
            justify-content: flex-end; /* Keeps text at the bottom */
            overflow: hidden;
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
        }

        /* Hero Image with Top Origin Alignment */
        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .hero-bg img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            /* TOP ORIGIN: Ensures faces/top of frame are visible */
            object-position: top center; 
            filter: contrast(110%) brightness(85%);
        }

        /* Cinematic Gradient Overlay */
        .overlay-gradient {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(
                to bottom,
                rgba(0, 0, 0, 0) 0%,
                rgba(0, 0, 0, 0.1) 30%,
                rgba(44, 24, 16, 0.7) 70%,
                rgba(18, 10, 7, 1) 100%
            );
            z-index: 2;
        }

        /* Film Texture Overlay */
        .texture-overlay {
            position: absolute; 
            top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.2;
            pointer-events: none;
            z-index: 3;
        }

        /* Content Area - Designed to handle optional title gracefully */
        .content-section {
            position: relative;
            z-index: 10;
            padding: 0 80px 180px 80px; 
            width: 100%;
        }

        .post-title {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: var(--title-size);
            text-transform: uppercase;
            letter-spacing: -2px;
            line-height: 0.95;
            text-shadow: 0 15px 40px rgba(0,0,0,0.9);
        }

        span.highlight {
            color: var(--cinema-yellow);
        }

        /* Branding Footer */
        .footer-brand {
            position: absolute;
            bottom: 60px;
            left: 0;
            width: 100%;
            text-align: center;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.4);
            font-size: 24px;
            letter-spacing: 12px;
            text-transform: uppercase;
            z-index: 10;
        }

        /* Inner Cinema Frame */
        .inner-border {
            position: absolute;
            top: 40px;
            bottom: 40px;
            left: 40px;
            right: 40px;
            border: 1px solid rgba(255, 179, 0, 0.2);
            pointer-events: none;
            z-index: 4;
        }
    </style>
</head>
<body>

    <div class="post-container">
        <!-- Hero Image (Centered at Top) -->
        <div class="hero-bg">
            <img src="https://images.unsplash.com/photo-1536440136628-849c177e76a1?q=80&w=1080&auto=format&fit=crop" alt="Hero Image">
        </div>

        <!-- Layers -->
        <div class="overlay-gradient"></div>
        <div class="texture-overlay"></div>
        <div class="inner-border"></div>

        <!-- Content Section -->
        <div class="content-section">
            <h1 class="post-title">
                The <span class="highlight">Cinematic</span> Masterpiece
            </h1>
        </div>

        <!-- Footer -->
        <div class="footer-brand">@cinefriendsofficial</div>
    </div>

</body>
</html>