<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Film Strip - Edge Blending Pro</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Oswald:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
            --title-size: {{#if titleFontSize}}{{titleFontSize}}px{{else}}60px{{/if}};
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start; 
            overflow: hidden;
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
        }

        .bg-image-layer {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: cover;
            background-position: left center;
            filter: grayscale(100%) brightness(0.5); 
            opacity: 0.18; 
            z-index: 1;
        }

        .header-section {
            margin-top: 80px;
            margin-bottom: 50px;
            width: 900px;
            text-align: center;
            z-index: 10;
        }

        .post-title {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: var(--title-size);
            text-transform: uppercase;
            letter-spacing: 1px;
            line-height: 1.1;
        }

        .stack-wrapper {
            width: 920px;
            display: flex;
            flex-direction: column;
            gap: 30px;
            z-index: 10;
        }

        .no-title-spacing { margin-top: 120px; }

        .stack-item {
            height: 320px; 
            width: 100%;
            position: relative;
            overflow: hidden;
            border: 6px solid var(--cinema-yellow);
            background-color: #000;
            display: flex;
            justify-content: center; 
            align-items: center;
        }

        .item-bg-blur {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background-size: cover;
            background-position: center;
            filter: blur(40px) brightness(0.3);
            transform: scale(1.3);
            z-index: 1;
        }

        /* Image remains at 70% min-width as requested */
        .stack-item img.main-img {
            position: relative;
            height: 100%;
            min-width: 70%; 
            max-width: 100%; 
            object-fit: cover; 
            z-index: 2;
        }

        /* UPDATED GRADIENT LOGIC */
        .item-edge-blend {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            z-index: 3;
            background: 
                /* 
                   Left: Solid black for 20% to swallow the image edge, 
                   then fades to transparent at 45% 
                */
                linear-gradient(to right, #000 0%, #000 20%, transparent 45%),
                /* 
                   Right: Solid black for 20%, fades to transparent at 45% 
                */
                linear-gradient(to left, #000 0%, #000 20%, transparent 45%),
                /* Top/Bottom masks */
                linear-gradient(to top, #000 0%, transparent 15%),
                linear-gradient(to bottom, #000 0%, transparent 15%);
            pointer-events: none;
        }

        .item-gradient {
            position: absolute;
            bottom: 0; left: 0; width: 100%; height: 70%;
            background: linear-gradient(to top, rgba(0,0,0,0.9) 0%, transparent 100%);
            z-index: 4;
        }

        .item-caption {
            position: absolute;
            bottom: 15px; left: 25px; right: 25px;
            z-index: 5;
            color: var(--text-light);
            font-family: var(--font-main);
            font-size: 26px;
            font-weight: 600;
            text-transform: uppercase;
            text-shadow: 2px 2px 8px #000;
        }

        span.highlight { color: var(--cinema-yellow); }

        .footer-brand {
            position: absolute;
            bottom: 50px;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.3);
            font-size: 24px;
            letter-spacing: 8px;
            text-transform: uppercase;
            z-index: 10;
        }
    </style>
</head>

<script>
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <div class="bg-image-layer"></div>

        {{#if title}}
        <div class="header-section">
            <h1 class="post-title">{{{highlight title}}}</h1>
        </div>
        {{/if}}

        <div class="stack-wrapper {{#unless title}}no-title-spacing{{/unless}}">
            
            {{#each images}}
            <div class="stack-item">
                <div class="item-bg-blur" style="background-image: url('{{this}}');"></div>
                
                <img src="{{this}}" class="main-img" alt="Scene {{@index}}">
                
                <!-- New intense edge blend -->
                <div class="item-edge-blend"></div>

                {{#if ../captions}}
                    {{#with (lookup ../captions @index) as |currentCaption|}}
                        {{#if currentCaption}}
                        <div class="item-gradient"></div>
                        <p class="item-caption">{{{highlight currentCaption}}}</p>
                        {{/if}}
                    {{/with}}
                {{/if}}
            </div>
            {{/each}}

        </div>

        <div class="footer-brand">@cinefriendsofficial</div>
    </div>

</body>
</html>