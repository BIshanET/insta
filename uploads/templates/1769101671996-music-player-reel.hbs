<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{pageTitle}}</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;900&family=Oswald:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            /* Theme Colors */
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            
            /* Logic for the Player */
            --glass-bg: rgba(255, 255, 255, 0.92);
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: var(--font-main);
        }

        .page-container {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            width: 100%;
            overflow: hidden;
        }

        .preview-scale-wrapper {
            display: flex;
            justify-content: center;
            align-items: center;
            transform: scale(0.45); 
        }

        /* --- INSTAGRAM REEL DIMENSIONS (1080x1920) --- */
        .insta-post {
            width: 1080px;
            height: 1920px;
            position: relative;
            /* Variable: Main Image */
            background-image: url('{{mainImage}}');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            overflow: hidden;
        }

        /* Background Texture & Gradient Overlay */
        .insta-post::after {
            content: '';
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(to bottom, rgba(44,24,16,0.85), rgba(44,24,16,0.98)),
                        url('https://www.transparenttextures.com/patterns/stardust.png');
            z-index: 1;
        }

        /* Player Card */
        .player-card {
            position: relative;
            z-index: 10;
            width: 850px; 
            background: var(--glass-bg);
            backdrop-filter: blur(40px);
            -webkit-backdrop-filter: blur(40px);
            border-radius: 60px;
            padding: 70px 60px;
            box-shadow: 0 40px 80px rgba(0,0,0,0.5);
            border-bottom: 12px solid var(--cinema-yellow);
        }

        /* Album Art */
        .album-art {
            width: 100%;
            height: 730px;
            border-radius: 40px;
            /* Variable: Main Image */
            background-image: url('{{mainImage}}');
            background-size: cover;
            background-position: center;
            margin-bottom: 60px;
            border: 6px solid var(--cinema-brown);
        }

        /* Typography */
        .song-info {
            margin-bottom: 60px;
            text-align: center;
        }

        .playlist-label {
            font-family: var(--font-display);
            font-size: 64px;
            font-weight: 700;
            color: var(--cinema-brown);
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .song-title {
            font-size: 32px;
            font-weight: 600;
            color: var(--cinema-brown);
            opacity: 0.9;
        }

        /* Progress Bar */
        .progress-container {
            width: 100%;
            height: 12px;
            background: rgba(44, 24, 16, 0.15);
            border-radius: 6px;
            position: relative;
            margin-bottom: 80px;
        }

        .progress-bar {
            /* Variable: Progress Percent */
            width: {{progressPercent}}%;
            height: 100%;
            background: var(--cinema-brown);
            border-radius: 6px;
        }

        .progress-knob {
            position: absolute;
            top: 50%;
            /* Variable: Progress Percent */
            left: {{progressPercent}}%;
            transform: translate(-50%, -50%);
            width: 36px;
            height: 36px;
            background: var(--cinema-yellow);
            border: 4px solid var(--cinema-brown);
            border-radius: 50%;
        }

        /* Controls */
        .controls {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 20px;
        }

        .icon {
            fill: var(--cinema-brown);
            width: 58px;
            height: 58px;
            cursor: pointer;
        }

        .play-button {
            width: 150px;
            height: 150px;
            background: var(--cinema-brown);
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            cursor: pointer;
            box-shadow: 0 15px 30px rgba(44, 24, 16, 0.4);
        }

        .play-button .icon {
            fill: var(--cinema-yellow);
            width: 70px;
            height: 70px;
        }

        /* Footer Handle */
        .handle {
            position: absolute;
            bottom: 120px;
            z-index: 10;
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            font-size: 42px;
            font-weight: 700;
            letter-spacing: 4px;
            text-transform: uppercase;
            text-shadow: 0 4px 20px rgba(0,0,0,0.8);
        }
    </style>
</head>
<body>

    <div class="page-container">
        
        <div class="preview-scale-wrapper">
            
            <div class="insta-post">
                
                <div class="player-card">
                    <div class="album-art"></div>
                    
                    <div class="song-info">
                        <div class="playlist-label">{{song}}</div>
                        <div class="song-title">{{movie}}</div>
                    </div>

                    <div class="progress-container">
                        <div class="progress-bar"></div>
                        <div class="progress-knob"></div>
                    </div>

                    <div class="controls">
                        <!-- Shuffle -->
                        <svg class="icon" viewBox="0 0 24 24"><path d="M10.59 9.17L5.41 4 4 5.41l5.17 5.17 1.42-1.41zM14.5 4l2.04 2.04L4 18.59 5.41 20 17.96 7.45 20 9.5V4h-5.5zm.59 9.41l-1.41 1.41 3.13 3.13L14.5 20H20v-5.5l-2.04 2.04-3.37-3.37z"/></svg>
                        
                        <!-- Prev -->
                        <svg class="icon" viewBox="0 0 24 24"><path d="M6 6h2v12H6zm3.5 6l8.5 6V6z"/></svg>
                        
                        <!-- Pause (Dynamic) -->
                        <div class="play-button">
                            <svg class="icon" viewBox="0 0 24 24"><path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"/></svg>
                        </div>
                        
                        <!-- Next -->
                        <svg class="icon" viewBox="0 0 24 24"><path d="M6 18l8.5-6L6 6v12zM16 6v12h2V6h-2z"/></svg>
                        
                        <!-- Repeat -->
                        <svg class="icon" viewBox="0 0 24 24"><path d="M7 7h10v3l4-4-4-4v3H5v6h2V7zm10 10H7v-3l-4 4 4 4v-3h12v-6h-2v4z"/></svg>
                    </div>
                </div>

                <!-- Static Handle -->
                <div class="handle">@cinefriendsofficial</div>
                
            </div>
            
        </div>
        
    </div>

</body>
</html>