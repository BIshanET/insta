<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Editorial Quotes - HBS</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Oswald:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        /* --- POST CONTAINER (1080x1350) --- */
        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            overflow: hidden;
            display: flex;
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
        }

        /* Texture Overlay */
        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.3;
            pointer-events: none;
            z-index: 50;
        }

        /* Background Logo Foundation */
        .logo-bg {
            position: absolute;
            bottom: -100px;
            right: -100px;
            width: 800px;
            height: 800px;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: contain;
            background-repeat: no-repeat;
            filter: sepia(1) brightness(0.4) contrast(1.2);
            opacity: 0.1;
            z-index: 1;
            transform: rotate(-15deg);
        }

        /* --- VERTICAL SIDEBAR (Conditional) --- */
        .sidebar {
            width: 140px;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.2);
            border-right: 2px solid rgba(255, 179, 0, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            z-index: 10;
        }

        .sidebar-text {
            font-family: var(--font-display);
            color: var(--cinema-yellow);
            font-size: 50px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 12px;
            transform: rotate(-180deg);
            writing-mode: vertical-lr;
            white-space: nowrap;
            opacity: 0.8;
        }

        /* --- MAIN CONTENT AREA --- */
        .content-area {
            flex: 1;
            display: flex;
            flex-direction: column;
            padding: 60px 60px 60px 40px;
            gap: 40px;
            position: relative;
            z-index: 10;
            justify-content: center;
        }

        /* Layout adjustment if Sidebar is hidden */
        .content-area.full-width {
            padding: 60px;
        }

        .quote-card {
            flex: 1;
            position: relative;
            border: 6px solid var(--cinema-yellow);
            border-radius: 4px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0,0,0,0.5);
            background-color: #000;
            max-height: 520px; /* Prevents cards from being too tall with fewer items */
        }

        .card-img {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            object-fit: cover;
            opacity: 0.7;
            filter: contrast(1.1) brightness(0.8);
        }

        .card-gradient {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: linear-gradient(to top, rgba(0,0,0,0.9) 0%, transparent 60%);
            z-index: 2;
        }

        .card-content {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            padding: 30px 40px;
            z-index: 3;
        }

        .quote-text {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: 42px;
            line-height: 1.1;
            text-transform: uppercase;
            font-weight: 700;
            margin-bottom: 10px;
            letter-spacing: -1px;
        }

        .quote-movie {
            font-family: var(--font-main);
            color: var(--cinema-yellow);
            font-size: 20px;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 4px;
        }

        /* Handle Branding */
        .handle {
            position: absolute;
            bottom: 30px;
            right: 60px;
            font-family: var(--font-display);
            color: var(--cinema-yellow);
            font-size: 22px;
            font-weight: 500;
            letter-spacing: 4px;
            opacity: 0.4;
            z-index: 100;
        }

        .highlight {
            color: var(--cinema-yellow);
        }

    </style>
</head>

<script>
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <div class="texture-overlay"></div>
        <div class="logo-bg"></div>

        <!-- Conditional Sidebar -->
        {{#if sidebar_title}}
        <div class="sidebar">
            <h2 class="sidebar-text">{{sidebar_title}}</h2>
        </div>
        {{/if}}

        <!-- Main Content Area -->
        <div class="content-area {{#unless sidebar_title}}full-width{{/unless}}">
            
            {{#each quotes}}
            <div class="quote-card">
                <!-- Background lookup -->
                <img src="{{lookup ../images @index}}" class="card-img" alt="Scene">
                
                <div class="card-gradient"></div>
                
                <div class="card-content">
                    <p class="quote-text">{{{highlight this}}}</p>
                    <!-- Movie name lookup -->
                    <p class="quote-movie">{{lookup ../movies @index}}</p>
                </div>
            </div>
            {{/each}}

        </div>

        <div class="handle">{{#if handle}}{{handle}}{{else}}@cinefriendsofficial{{/if}}</div>
    </div>

</body>
</html>