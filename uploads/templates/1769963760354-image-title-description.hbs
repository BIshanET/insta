<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends News Template</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&family=Oswald:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-brown-light: #3d2216;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        /* 1080x1350 Portrait Container */
        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            overflow: hidden;
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
            justify-content: space-around;
        }

        /* Background Layer - left center position */
        .bg-image-layer {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: cover;
            background-position: left center;
            background-repeat: no-repeat;
            filter: grayscale(100%) brightness(0.6) contrast(120%); 
            opacity: 0.15; 
            z-index: 1;
        }

        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.3;
            pointer-events: none;
            z-index: 2;
        }

        .gradient-bottom {
            position: absolute;
            bottom: 0; left: 0; width: 100%; height: 35%;
            background: linear-gradient(to top, var(--cinema-brown) 0%, transparent 100%);
            z-index: 2;
        }

        /* Title Section */
        .header-section {
            margin-top: 90px;
            width: 880px; 
            text-align: center;
            z-index: 10;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .post-title {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: 50px; 
            text-transform: uppercase;
            letter-spacing: 0.5px;
            line-height: 1.2; 
            text-shadow: 0 5px 15px rgba(0,0,0,0.9);
        }

        /* Highlighting Style */
        .post-title span.highlight, 
        .post-description span.highlight {
            color: var(--cinema-yellow);
        }

        /* The Tag */
        .status-tag {
            margin-top: 30px;
            margin-bottom: 30px;
            padding: 8px 24px;
            border: 3px solid var(--cinema-yellow);
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            font-size: 24px;
            text-transform: uppercase;
            letter-spacing: 4px;
            border-radius: 4px;
            background: rgba(44, 24, 16, 0.6);
            font-weight: 700;
            z-index: 10;
        }

        /* Image Container */
        .image-container {
            position: relative;
            z-index: 10;
        }

        .image-frame {
            width: 820px; 
            height: 480px; 
            border: 2px solid rgba(255, 179, 0, 0.2);
            position: relative;
            padding: 12px;
            background: rgba(44, 24, 16, 0.85); 
        }

        .image-frame img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
            border: 6px solid var(--cinema-yellow);
        }

        .corner {
            position: absolute;
            width: 35px; height: 35px;
            border: 5px solid var(--cinema-yellow);
            z-index: 11;
        }
        .top-left { top: -8px; left: -8px; border-right: none; border-bottom: none; }
        .top-right { top: -8px; right: -8px; border-left: none; border-bottom: none; }
        .bot-left { bottom: -8px; left: -8px; border-right: none; border-top: none; }
        .bot-right { bottom: -8px; right: -8px; border-left: none; border-top: none; }

        /* Description Box */
        .content-box {
            margin-top: 50px;
            width: 820px;
            background: rgba(44, 24, 16, 0.88); 
            backdrop-filter: blur(8px);
            padding: 35px;
            border-left: 5px solid var(--cinema-yellow);
            z-index: 10;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
        }

        .post-description {
            color: var(--text-light);
            font-size: 28px;
            line-height: 1.5;
            font-weight: 400;
        }

        /* Branding Footer */
        .footer-brand {
            position: absolute;
            bottom: 50px;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.4);
            font-size: 22px;
            letter-spacing: 8px;
            text-transform: uppercase;
            z-index: 10;
        }
    </style>
</head>

<script>
  // Handlebars Helper Logic
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <div class="bg-image-layer"></div>
        <div class="texture-overlay"></div>
        <div class="gradient-bottom"></div>

        <div class="header-section">
            <!-- Triple braces allow HTML output from the highlight helper -->
            <h1 class="post-title">
                {{{highlight title}}}
            </h1>

            <!-- Only render tag if it exists -->
            {{#if tag}}
            <div class="status-tag">{{tag}}</div>
            {{/if}}
        </div>

        <div class="image-container">
            <div class="corner top-left"></div>
            <div class="corner top-right"></div>
            <div class="corner bot-left"></div>
            <div class="corner bot-right"></div>
            
            <div class="image-frame">
                <img src="{{main_image}}" alt="Movie Scene">
            </div>
        </div>

        <!-- Only render description box if it exists -->
        {{#if description}}
        <div class="content-box">
            <p class="post-description">
                {{{highlight description}}}
            </p>
        </div>
        {{/if}}

        <div class="footer-brand">@cinefriendsofficial</div>
    </div>

</body>
</html>