<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends News Template</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700;900&family=Oswald:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #1a1a1a;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* 1080x1350 Portrait Container */
        .instagram-post {
            width: 1080px; 
            height: 1350px;
            position: relative; 
            overflow: hidden;
            background-color: var(--cinema-brown);
            display: flex; 
            flex-direction: column;
            font-family: var(--font-main);
            box-shadow: 0 0 50px rgba(0,0,0,0.5);
        }

        /* --- BACKGROUND WATERMARK LAYERS --- */
        .bg-image-layer {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: cover;
            background-position: left center;
            background-repeat: no-repeat;
            filter: grayscale(100%) brightness(0.6) contrast(120%); 
            opacity: 0.15; 
            z-index: 1;
        }

        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.3;
            pointer-events: none;
            z-index: 2;
        }

        .gradient-bottom {
            position: absolute;
            bottom: 0; left: 0; width: 100%; height: 35%;
            background: linear-gradient(to top, var(--cinema-brown) 0%, transparent 100%);
            z-index: 2;
        }

        /* Content Layout */
        .content { 
            position: relative; 
            z-index: 10; 
            height: 100%; 
            display: flex; 
            flex-direction: column; 
            padding: 80px 70px 140px 70px; 
        }

        /* Header */
        .header { display: flex; align-items: center; gap: 20px; margin-bottom: 50px; }
        .handle { font-size: 24px; font-weight: 900; color: var(--cinema-yellow); letter-spacing: 5px; text-transform: uppercase; }
        .separator { flex: 1; height: 2px; background: rgba(255,179,0,0.3); }
        
        /* Dynamic Tag */
        .tag { 
            font-family: var(--font-display); 
            background: var(--cinema-yellow); 
            color: var(--cinema-brown); 
            padding: 10px 40px; 
            border-radius: 4px; 
            font-size: 42px; 
            text-transform: uppercase;
            font-weight: 700;
            letter-spacing: 2px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }

        /* Title with title_size variable */
        .news-title {
            font-family: var(--font-display);
            font-size: {{#if title_size}}{{title_size}}{{else}}78px{{/if}}; 
            line-height: 0.95; 
            color: var(--text-light);
            text-transform: uppercase; 
            margin-bottom: 45px;
            letter-spacing: -1px;
        }
        .news-title span {
            color: var(--cinema-yellow);
        }

        /* Image Gallery */
        .visual-container {
            width: 100%; 
            height: 520px; 
            display: flex; 
            gap: 20px; 
            margin-bottom: 50px;
        }

        .img-wrapper {
            flex: 1; 
            height: 100%; 
            position: relative;
            border: 6px solid var(--cinema-yellow);
            border-radius: 4px; 
            overflow: hidden;
            box-shadow: 0 25px 50px rgba(0,0,0,0.6);
        }

        .img-wrapper img { width: 100%; height: 100%; object-fit: cover; }
        .img-full-width { flex: 0 0 100%; }

        /* Description Box with description_size variable */
        .info-card {
            background: rgba(255,255,255,0.03);
            padding: 45px;
            border-radius: 4px;
            border-left: 8px solid var(--cinema-yellow);
            margin-top: auto;
        }

        .description {
            font-size: {{#if description_size}}{{description_size}}{{else}}30px{{/if}}; 
            line-height: 1.5; 
            color: var(--text-light);
            font-weight: 400; 
            opacity: 0.9;
        }
        .description span {
            color: var(--cinema-yellow);
            font-weight: 700;
        }
    </style>
</head>
<body>

    <div class="instagram-post">
        <!-- Background Layers -->
        <div class="bg-image-layer"></div>
        <div class="texture-overlay"></div>
        <div class="gradient-bottom"></div>

        <div class="content">
            <!-- Top Branding -->
            <div class="header">
                <span class="handle">@cinefriendsofficial</span>
                <div class="separator"></div>
                <!-- Dynamic Tag variable -->
                {{#if tag}}
                <span class="tag">{{tag}}</span>
                {{/if}}
            </div>

            <!-- News Title (Triple braces for HTML highlighting) -->
            <h1 class="news-title">
                {{{newsTitle}}}
            </h1>

            <!-- Image Section -->
            <div class="visual-container">
                <div class="img-wrapper {{#unless newsImage2}}img-full-width{{/unless}}">
                    <img src="{{newsImage1}}" alt="News Image 1">
                </div>
                
                {{#if newsImage2}}
                <div class="img-wrapper">
                    <img src="{{newsImage2}}" alt="News Image 2">
                </div>
                {{/if}}
            </div>

            <!-- Description Section (Triple braces for HTML highlighting) -->
            <div class="info-card">
                <p class="description">
                    {{{newsDescription}}}
                </p>
            </div>
        </div>
    </div>

</body>
</html>