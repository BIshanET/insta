<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Ultimate Rec</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;800&family=Oswald:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --text-dim: rgba(245, 245, 245, 0.75);
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #000;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .preview-scale-wrapper {
            transform-origin: center;
        }

        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
        }

        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.4;
            pointer-events: none;
            z-index: 2;
        }

        /* HERO IMAGE SECTION with DOUBLE GRADIENTS */
        .image-section {
            width: 100%;
            height: 520px; 
            position: relative;
            z-index: 1;
        }
        .movie-img {
            width: 100%; height: 100%;
            object-fit: cover;
            object-position: center 20%;
        }

        .gradient-top {
            position: absolute; top: 0; left: 0; width: 100%; height: 200px;
            background: linear-gradient(to bottom, rgba(44, 24, 16, 1) 0%, rgba(44, 24, 16, 0.8) 40%, transparent 100%);
            z-index: 5;
        }

        .gradient-bottom {
            position: absolute; bottom: 0; left: 0; width: 100%; height: 200px;
            background: linear-gradient(to top, var(--cinema-brown) 0%, transparent 100%);
            z-index: 5;
        }

        /* OVERLAYED HEADER */
        .rec-header {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 120px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 60px;
            z-index: 20; 
        }

        .rec-title-group { display: flex; align-items: center; gap: 20px; }
        .rec-icon { width: 44px; height: 44px; fill: var(--cinema-yellow); }
        .rec-text {
            font-family: var(--font-display);
            font-weight: 700;
            font-size: 38px;
            color: var(--cinema-yellow);
            text-transform: uppercase;
            letter-spacing: 3px;
        }
        .rec-count {
            font-family: var(--font-display);
            font-size: 38px;
            font-weight: 700;
            color: var(--cinema-yellow);
            padding: 2px 18px;
            border-radius: 8px;
            border: 4px solid var(--cinema-yellow);
        }

        /* DATA CONTAINER */
        .data-container {
            flex: 1;
            padding: 0 60px 40px 60px;
            display: flex;
            flex-direction: column;
            z-index: 10;
            margin-top: -40px; 
        }

        /* TOP SECTION logic */
        .top-info-split {
            display: flex;
            gap: 40px;
            margin-bottom: 40px;
            align-items: flex-start;
        }

        .left-info-col {
            flex: 1.4;
            display: flex;
            flex-direction: column;
        }

        /* Condition for 100% width when screenshots are missing */
        .left-info-col.full-width {
            flex: 1;
        }

        .movie-title {
            font-family: var(--font-display);
            font-size: 95px;
            line-height: 0.85;
            color: var(--text-light);
            text-transform: uppercase;
            margin-bottom: 20px;
        }

        .quick-stats {
            display: flex;
            gap: 20px;
            align-items: center;
            font-family: var(--font-main);
            color: var(--text-dim);
            font-size: 26px;
            font-weight: 600;
            margin-bottom: 25px;
        }
        .separator { width: 8px; height: 8px; background: var(--cinema-yellow); border-radius: 50%; }

        .genre-row { display: flex; gap: 14px; margin-bottom: 35px; flex-wrap: wrap; }
        .genre-badge {
            border: 3px solid var(--cinema-yellow);
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            padding: 6px 20px;
            border-radius: 8px;
            font-size: 24px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .mini-info-grid {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }
        .label {
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            font-size: 34px;
            text-transform: uppercase;
            font-weight: 700;
        }
        .value {
            color: var(--text-light);
            font-family: var(--font-display);
            font-size: 32px;
            font-weight: 500;
            line-height: 1.2;
        }

        /* RIGHT COLUMN: 2 Stacked Images */
        .right-gallery-col {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        .gallery-frame {
            width: 100%;
            height: 200px; 
            border: 3px solid var(--cinema-yellow);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0,0,0,0.4);
        }
        .gallery-frame img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* BOTTOM SECTION */
        .plot-container {
            width: 100%;
            margin-top: auto;
            margin-bottom: 30px;
        }
        .plot-text {
            font-family: var(--font-main);
            color: #FFF;
            font-size: 32px;
            line-height: 1.5;
            border-left: 8px solid var(--cinema-yellow);
            padding-left: 35px;
        }

        .handle {
            text-align: right;
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            font-size: 28px;
            letter-spacing: 5px;
            text-transform: uppercase;
            font-weight: 700;
            opacity: 0.6;
        }
    </style>
</head>

<script>
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span style="color: var(--cinema-yellow);">$1</span>')
      );
    });
  }
</script>

<body>

<div class="preview-scale-wrapper">
    <div class="post-container">
        <div class="texture-overlay"></div>

        <!-- Hero Section with Overlaid Header -->
        <div class="image-section">
            <div class="gradient-top"></div>
            
            <div class="rec-header">
                <div class="rec-title-group">
                    <svg class="rec-icon" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 14.5v-9l6 4.5-6 4.5z"/></svg>
                    <span class="rec-text">Movie Recommendation</span>
                </div>
                <div class="rec-count">{{number}}</div>
            </div>

            <img src="{{main_image}}" class="movie-img" alt="Movie Hero">
            
            <div class="gradient-bottom"></div>
        </div>

        <div class="data-container">
            
            <div class="top-info-split">
                
                <!-- If screenshots are empty, we add 'full-width' class -->
                <div class="left-info-col {{#unless screenshots}}full-width{{/unless}}">
                    <h1 class="movie-title">{{{highlight movie_name}}}</h1>

                    <div class="quick-stats">
                        <span>{{year}}</span>
                        <div class="separator"></div>
                        <span>{{duration}}</span>
                    </div>

                    <div class="genre-row">
                        {{#each genres}}
                        <span class="genre-badge">{{this}}</span>
                        {{/each}}
                    </div>

                    <div class="mini-info-grid">
                        <div class="grid-item">
                            <span class="label">Director:</span>
                            <span class="value">{{director}}</span>
                        </div>
                        <div class="grid-item">
                            <span class="label">Starring:</span>
                            <span class="value">{{stars}}</span>
                        </div>
                    </div>
                </div>

                <!-- Only render the gallery if screenshots exist -->
                {{#if screenshots}}
                <div class="right-gallery-col">
                    {{#each screenshots}}
                    <div class="gallery-frame">
                        <img src="{{this}}" alt="Scene">
                    </div>
                    {{/each}}
                </div>
                {{/if}}

            </div>

            <div class="plot-container">
                <p class="plot-text">
                    {{{highlight description}}}
                </p>
            </div>
            
            <div class="handle">@cinefriendsofficial</div>

        </div>
    </div>
</div>

</body>
</html>