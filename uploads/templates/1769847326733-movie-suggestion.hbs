<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Ultimate Rec - HBS Premium</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;800&family=Oswald:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --text-dim: rgba(245, 245, 245, 0.75);
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
            
            /* Dynamic Font Size Logic */
            --title-size-val: {{#if title_size}}{{title_size}}px{{else}}95px{{/if}};
            --desc-size-val: {{#if description_size}}{{description_size}}px{{else}}32px{{/if}};
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #000;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            box-shadow: 0 50px 100px rgba(0,0,0,0.9);
        }

        /* PREMIUM BACKGROUND LOGO */
        .logo-blended {
            position: absolute;
            bottom: -150px;
            left: 7%;
            transform: translateX(-50%);
            width: 1400px;
            height: 1000px;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center bottom;
            filter: sepia(1) saturate(5) hue-rotate(-15deg) brightness(0.6) contrast(1.2);
            mix-blend-mode: screen; 
            opacity: 0.15;
            z-index: 2;
        }

        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.3;
            pointer-events: none;
            z-index: 3;
        }

        /* HERO IMAGE SECTION */
        .image-section {
            width: 100%;
            height: 520px; 
            position: relative;
            z-index: 4;
        }
        .movie-img {
            width: 100%; height: 100%;
            object-fit: cover;
            object-position: center 20%;
        }

        .gradient-top {
            position: absolute; top: 0; left: 0; width: 100%; height: 200px;
            background: linear-gradient(to bottom, rgba(44, 24, 16, 1) 0%, rgba(44, 24, 16, 0.8) 40%, transparent 100%);
            z-index: 5;
        }

        .gradient-bottom {
            position: absolute; bottom: 0; left: 0; width: 100%; height: 200px;
            background: linear-gradient(to top, var(--cinema-brown) 0%, transparent 100%);
            z-index: 5;
        }

        /* OVERLAYED HEADER */
        .rec-header {
            position: absolute;
            top: 0; left: 0; width: 100%;
            height: 120px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 60px;
            z-index: 20; 
        }

        .rec-title-group { display: flex; align-items: center; gap: 20px; }
        .rec-icon { width: 44px; height: 44px; fill: var(--cinema-yellow); }
        .rec-text {
            font-family: var(--font-display);
            font-weight: 700;
            font-size: 38px;
            color: var(--cinema-yellow);
            text-transform: uppercase;
            letter-spacing: 3px;
        }

        /* DATA CONTAINER */
        .data-container {
            flex: 1;
            padding: 0 60px 40px 60px;
            display: flex;
            flex-direction: column;
            position: relative;
            z-index: 10; 
            margin-top: -40px; 
        }

        .top-info-split {
            display: flex;
            gap: 40px;
            margin-bottom: 40px;
            align-items: flex-start;
        }

        .left-info-col { flex: 1.4; display: flex; flex-direction: column; }
        .left-info-col.full-width { flex: 1; }

        .movie-title {
            font-family: var(--font-display);
            font-size: var(--title-size-val);
            line-height: 0.85;
            color: var(--text-light);
            text-transform: uppercase;
            margin-bottom: 20px;
            text-shadow: 0 5px 15px rgba(0,0,0,0.5);
        }

        .quick-stats {
            display: flex;
            gap: 20px;
            align-items: center;
            font-family: var(--font-main);
            color: var(--text-dim);
            font-size: 26px;
            font-weight: 600;
            margin-bottom: 25px;
        }
        .separator { width: 8px; height: 8px; background: var(--cinema-yellow); border-radius: 50%; }

        .genre-row { display: flex; gap: 14px; margin-bottom: 35px; flex-wrap: wrap; }
        .genre-badge {
            border: 3px solid var(--cinema-yellow);
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            padding: 6px 20px;
            border-radius: 8px;
            font-size: 24px;
            font-weight: 600;
            text-transform: uppercase;
        }

        .mini-info-grid { display: flex; flex-direction: column; gap: 20px; }
        .label {
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            font-size: 34px;
            text-transform: uppercase;
            font-weight: 700;
        }
        .value {
            color: var(--text-light);
            font-family: var(--font-display);
            font-size: 32px;
            font-weight: 500;
            line-height: 1.2;
        }

        /* RIGHT GALLERY */
        .right-gallery-col { flex: 1; display: flex; flex-direction: column; gap: 15px; }
        .gallery-frame {
            width: 100%; height: 200px; 
            border: 3px solid var(--cinema-yellow);
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(0,0,0,0.4);
            background: #000;
        }
        .gallery-frame img { width: 100%; height: 100%; object-fit: cover; }

        /* BOTTOM PLOT */
        .plot-container { width: 100%; margin-top: auto; margin-bottom: 30px; }
        .plot-text {
            font-family: var(--font-main);
            color: #FFF;
            font-size: var(--desc-size-val);
            line-height: 1.5;
            border-left: 8px solid var(--cinema-yellow);
            padding-left: 35px;
            text-shadow: 0 2px 10px rgba(0,0,0,0.5);
        }

        .footer-branding {
            display: flex;
            justify-content: flex-end;
            width: 100%;
        }

        .handle {
            color: var(--cinema-yellow);
            font-family: var(--font-display);
            font-size: 28px;
            letter-spacing: 5px;
            text-transform: uppercase;
            font-weight: 700;
            opacity: 0.4;
        }

        .highlight {
            color: var(--cinema-yellow);
        }
    </style>
</head>

<script>
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <!-- Premium Layers -->
        <div class="logo-blended"></div>
        <div class="texture-overlay"></div>

        <!-- Hero Section -->
        <div class="image-section">
            <div class="gradient-top"></div>
            
            <div class="rec-header">
                <div class="rec-title-group">
                    <svg class="rec-icon" viewBox="0 0 24 24"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 14.5v-9l6 4.5-6 4.5z"/></svg>
                    <span class="rec-text">Movie Recommendation</span>
                </div>
            </div>

            <img src="{{main_image}}" class="movie-img" alt="Movie Hero">
            
            <div class="gradient-bottom"></div>
        </div>

        <!-- Content Area -->
        <div class="data-container">
            
            <div class="top-info-split">
                <!-- Conditional full-width class if no screenshots -->
                <div class="left-info-col {{#unless screenshots}}full-width{{/unless}}">
                    <h1 class="movie-title">{{{highlight movie_name}}}</h1>

                    <div class="quick-stats">
                        <span>{{year}}</span>
                        <div class="separator"></div>
                        <span>{{duration}}</span>
                    </div>

                    <div class="genre-row">
                        {{#each genres}}
                        <span class="genre-badge">{{this}}</span>
                        {{/each}}
                    </div>

                    <div class="mini-info-grid">
                        <div class="grid-item">
                            <span class="label">Director:</span>
                            <span class="value">{{director}}</span>
                        </div>
                        <div class="grid-item">
                            <span class="label">Starring:</span>
                            <span class="value">{{stars}}</span>
                        </div>
                    </div>
                </div>

                {{#if screenshots}}
                <div class="right-gallery-col">
                    {{#each screenshots}}
                    <div class="gallery-frame">
                        <img src="{{this}}" alt="Scene">
                    </div>
                    {{/each}}
                </div>
                {{/if}}
            </div>

            <div class="plot-container">
                <p class="plot-text">
                    {{{highlight description}}}
                </p>
            </div>
            
            <div class="footer-branding">
                <div class="handle">@cinefriendsofficial</div>
            </div>

        </div>
    </div>

</body>
</html>