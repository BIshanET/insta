<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cinefriends Film Strip Template</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&family=Oswald:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --cinema-brown: #2C1810;
            --cinema-yellow: #FFB300;
            --text-light: #F5F5F5;
            --font-main: 'Inter', sans-serif;
            --font-display: 'Oswald', sans-serif;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: #120a07;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            font-family: var(--font-main);
        }

        /* 1080x1350 Portrait Container */
        .post-container {
            width: 1080px;
            height: 1350px;
            background-color: var(--cinema-brown);
            position: relative;
            display: flex;
            flex-direction: column;
            align-items: center;
            overflow: hidden;
            box-shadow: 0 50px 100px rgba(0,0,0,0.8);
        }

        /* Static Background Layer - Left Center Position */
        .bg-image-layer {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://i.ibb.co/qMZ7cskj/output-onlinepngtools-1.jpg');
            background-size: cover;
            background-position: left center;
            background-repeat: no-repeat;
            filter: grayscale(100%) brightness(0.5) contrast(120%); 
            opacity: 0.18; 
            z-index: 1;
        }

        .texture-overlay {
            position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: url('https://www.transparenttextures.com/patterns/stardust.png');
            opacity: 0.3;
            pointer-events: none;
            z-index: 2;
        }

        /* Header / Title - Conditional Spacing */
        .header-section {
            margin-top: 60px;
            margin-bottom: 50px;
            width: 900px;
            text-align: center;
            z-index: 10;
        }

        .post-title {
            font-family: var(--font-display);
            color: var(--text-light);
            font-size: 60px;
            text-transform: uppercase;
            letter-spacing: 1px;
            line-height: 1.1;
            text-shadow: 0 10px 20px rgba(0,0,0,0.9);
        }

        /* VERTICAL STACK CONTAINER */
        .stack-wrapper {
            width: 920px;
            display: flex;
            flex-direction: column;
            gap: 30px;
            z-index: 10;
            margin-bottom: 100px; 
        }

        /* If title is missing, add top padding to the wrapper */
        .no-title-spacing {
            margin-top: 80px;
        }

        /* Individual Stack Item */
        .stack-item {
            height: 300px; 
            width: 100%;
            position: relative;
            overflow: hidden;
            border: 6px solid var(--cinema-yellow);
            box-shadow: 0 15px 35px rgba(0,0,0,0.6);
            background-color: #000;
        }

        .stack-item img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            display: block;
        }

        /* Image Gradient & Caption */
        .item-gradient {
            position: absolute;
            bottom: 0; left: 0; width: 100%; height: 70%;
            background: linear-gradient(to top, rgba(0,0,0,0.95) 0%, transparent 100%);
            z-index: 2;
        }

        .item-caption {
            position: absolute;
            bottom: 15px; left: 25px; right: 25px;
            z-index: 3;
            color: var(--text-light);
            font-family: var(--font-main);
            font-size: 26px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
        }

        /* Highlight Span */
        span.highlight {
            color: var(--cinema-yellow);
        }

        /* Footer Branding */
        .footer-brand {
            position: absolute;
            bottom: 50px;
            font-family: var(--font-display);
            color: rgba(255, 255, 255, 0.3);
            font-size: 24px;
            letter-spacing: 8px;
            text-transform: uppercase;
            z-index: 10;
        }
    </style>
</head>

<script>
  if (window.Handlebars) {
    Handlebars.registerHelper("highlight", function (text) {
      if (!text) return "";
      return new Handlebars.SafeString(
        text.replace(/\|(.*?)\|/g, '<span class="highlight">$1</span>')
      );
    });
  }
</script>

<body>

    <div class="post-container">
        <div class="bg-image-layer"></div>
        <div class="texture-overlay"></div>

        <!-- Conditional Title Section -->
        {{#if title}}
        <div class="header-section">
            <h1 class="post-title">{{{highlight title}}}</h1>
        </div>
        {{/if}}

        <!-- Vertical Stack Loop -->
        <div class="stack-wrapper {{#unless title}}no-title-spacing{{/unless}}">
            
            {{#each images}}
            <div class="stack-item">
                <img src="{{this}}" alt="Scene {{@index}}">
                
                {{#if ../captions}}
                    {{#with (lookup ../captions @index) as |currentCaption|}}
                        {{#if currentCaption}}
                        <div class="item-gradient"></div>
                        <p class="item-caption">{{{highlight currentCaption}}}</p>
                        {{/if}}
                    {{/with}}
                {{/if}}
            </div>
            {{/each}}

        </div>

        <div class="footer-brand">@cinefriendsofficial</div>
    </div>

</body>
</html>